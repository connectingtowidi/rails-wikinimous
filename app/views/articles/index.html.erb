<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="px-3">
    <div class="d-flex justify-content-between">
      <%= link_to "Wikinimous", root_path, style:"display: block; font-size: 24px; text-decoration: none; color: grey;" %>
      <div>
        <%= link_to "New Article", new_article_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</nav>

<div class="container mt-3">
  <div class="row">
    <div class="col-sm-8 offset-sm-2">
      <% @articles.each do |article| %>
        <%= link_to article.title, article_path(article), style:"color: #007bff; font-size: 30px" %>
        <p><%= article.content %></p>
      <% end %>
    </div>
  </div>
</div>
